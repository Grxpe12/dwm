" Execute Pathogen plugin manager.
execute pathogen#infect()

" Enable plugins and load them and loads and indent file for the detected file
" tipe.
filetype plugin indent on

" Disable compatiblity with vi.
set nocompatible

" Enable type file detection.
filetype on

" Enable line numbering.
set number

" Enable syntax highlighting
syntax on

" Use space characters instead of tabs.
set expandtab

" Ignore capital letters when seraching.
set ignorecase

" Show matching words during search
set showmatch

" Use highlighting when seraching.
set hlsearch

" Show partial command in the corner.
set showcmd

" Show the mode I am in.
set showmode

